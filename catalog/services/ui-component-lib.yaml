apiVersion: waypoint/v1
kind: Service
metadata:
  name: ui-component-lib
  description: Reusable UI component library for consistent design system across applications
  tags: [frontend, ui, design-system, components]
spec:
  owner: team-platform
  lifecycle: experimental
  system: platform
  dependsOn: []
  providesApis: []
